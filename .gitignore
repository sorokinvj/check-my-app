# dependencies
node_modules/

# next.js
.next/
out/

# production
build/
dist/

# env
.env
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# prisma
prisma/*.db

# evidence artifacts (screenshots, transcripts, generated specs).
# Root-anchored: a bare "evidence/" also swallowed src/app/api/evidence/ — the
# evidence API route was never committed and the first CI-built deploy shipped
# without it (every screenshot 404'd, 2026-08-16).
/evidence/
/storage/
generated-tests/

# typescript
*.tsbuildinfo
next-env.d.ts

# playwright
test-results/
playwright-report/

# claude harness internals
.claude/scheduled_tasks.lock
spikes/*/node_modules/
spikes/*/.dev.vars
spikes/*/.wrangler/
spikes/*/.open-next/
spikes/*/.next/

# generated prisma client (workerd)
src/generated/prisma/
.dev.vars

# OpenNext build output (contains baked env — never commit)
.open-next/
.wrangler/

# clerk configuration (can include secrets)
/.clerk/

# Local Redis dump (brew redis in this dir)
dump.rdb
